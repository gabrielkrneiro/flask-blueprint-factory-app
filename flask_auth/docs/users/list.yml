tags:
  - User

definitions:
  User:
    type: object
    properties:
      id:
        type: integer
        format: int64
      login:
        type: string
      password:
        type: string

responses:
  200:
    description: Return the list of users
    schema:
      type: array
      items:
        type: object
        properties:
          id:
            type: integer
            format: int64
          login:
            type: string
