definitions:
  ServerIsRunning:
    type: object
    properties:
      message:
        type: string
      documentation:
        type: string

responses:
  200:
    description: Return an object if server is running
    schema:
      $ref: "#/definitions/ServerIsRunning"
    examples:
      rgb: ["red", "green", "blue"]
